import { constants } from './constants'
export const Footer = () => {
  return (
    <footer className='border-t-2 px-7 py-2 w-full flex justify-end font-light text-sm'>{constants.textFooter}</footer>
  )
}